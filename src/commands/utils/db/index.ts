import {Command} from '@oclif/command'

export default class DatabaseIndex extends Command {
  static description = 'interact with SQL databases (only Postgres supported)'

  async run() {
    // void...
  }
}
